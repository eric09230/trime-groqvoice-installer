# 📱 手機安裝說明（免電腦・免工具）

> 給別人裝最實際的方式：只要把檔案傳過去，照下面點一點就好，**不用電腦、不用 Termux、不用 Shizuku**。

## 你需要的東西
- 一支 Android 手機
- 三樣檔案：`trime.apk`、`groqvoice.apk`、`rime` 資料夾（整包傳過去，例如用 LINE / Quick Share / 雲端）

---

## 步驟

### 1️⃣ 安裝兩個 App
用檔案管理員點 `trime.apk` → 安裝（若擋下來，允許「安裝未知應用程式」再裝）。
同樣方式裝 `groqvoice.apk`。

### 2️⃣ 放入設定檔（rime 資料夾）
用檔案管理員把整個 **`rime` 資料夾**，複製到**手機內部儲存的最外層**。
結果路徑要是：`內部儲存/rime/`（也就是 `/storage/emulated/0/rime/`）。

### 3️⃣ 開權限（⚠️ 這步沒做 Trime 會閃退）
- **設定 → 應用程式 → 同文輸入法(Trime) → 權限 → 檔案和媒體 →「允許存取所有檔案」**
- **設定 → 應用程式 → GroqVoice → 權限 → 麥克風 → 允許**

### 4️⃣ 啟用鍵盤
**設定 → 一般管理（或系統）→ 語言與輸入 → 螢幕鍵盤 / 管理鍵盤**
把「**同文輸入法**」和「**GroqVoice**」都打開。

### 5️⃣ 切換到同文輸入法
隨便開一個輸入框 → 點右下角的鍵盤切換鈕 → 選「同文輸入法」。

### 6️⃣ 設定方案與外觀
開 **同文輸入法 (Trime) App**：
- 「**方案**」→ 右下角 ＋ → 選「**洋蔥手機注音plus**」（用鉛筆移除多餘的）
- 「**鍵盤樣式 → 主題**」→ 選「**洋蔥注音331k_M**」
- 「**配色**」→ 挑一個喜歡的
- 右上角「**重新部署**」→ 等跑完

### 7️⃣ 設定語音輸入
- 開 **GroqVoice App** → 貼上你的 Groq API Key（免費申請：https://console.groq.com ）
- 在 Trime「常規 → 首選語音輸入」選 **GroqVoice**

### ✅ 完成！測試
打開聊天 App → 用同文鍵盤 → **長按 Enter** → 開始語音輸入 → 講完按 Stop。

---

## 常見問題

| 狀況 | 解法 |
|------|------|
| Trime 一直閃退 | 第 3 步沒給「所有檔案」權限，回去開 |
| 鍵盤沒出現 | 第 4 步沒啟用，或第 5 步沒切過去 |
| 長按 Enter 說「尚未安裝語音輸入」 | GroqVoice 沒啟用，或沒在「首選語音輸入」選它 |
| 中/英切換很慢 | 你裝到「滑動版」了，請改用無滑動的 `trime.apk` |
| 打字沒反應 / 沒候選字 | 回 Trime 點「重新部署」 |
